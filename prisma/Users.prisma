model Users {
  id       String     @id @default(uuid())
  name     String
  email    String     @unique
  password String
  bio      String?
  phone    String?
  avatar   String?
  products products[]
}
