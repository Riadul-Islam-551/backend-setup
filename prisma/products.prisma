model products {
  id          String @id @default(uuid())
  userId      String
  name        String
  category    String
  description String
  price       Int
  users       Users  @relation(fields: [userId], references: [id])
}
